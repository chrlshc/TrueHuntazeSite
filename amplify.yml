version: 1
applications:
  - appRoot: site-web
    frontend:
      phases:
        preBuild:
          commands:
            - nvm use 20 || echo "Using default Node"
            - npm ci
        build:
          commands:
            - npm run build
      artifacts:
        baseDirectory: site-web/.next
        files:
          - '**/*'
      cache:
        paths:
          - site-web/node_modules/**/*